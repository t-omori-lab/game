# Rename this file to `config.toml` only when project-scoped Codex settings are needed.
# Project-scoped config is loaded only for trusted projects.
# Keep credentials and machine-specific authentication out of this file.
